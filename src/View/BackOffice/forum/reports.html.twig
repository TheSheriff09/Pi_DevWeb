<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Forum Admin - Reports</title>
    <link rel="stylesheet" href="{{ asset('Back/css/styles.css') }}?v=2">
</head>
<body>
    <script>
        if (localStorage.getItem('admin_theme') === 'dark') {
            document.body.classList.add('dark-mode');
        }
    </script>

    <!-- SIDEBAR FOR FORUM MODULE -->
    <aside class="sidebar">
        <div class="sidebar-header">
            <h2>Forum Admin</h2>
        </div>
        <ul class="sidebar-menu">
            <li><a href="{{ path('app_admin_panel') }}" style="color: #ff9d9d;">&larr; Main Admin</a></li>
            <li><a href="{{ path('app_admin_forum_dashboard') }}" class="{{ current_menu == 'dashboard' ? 'active' : '' }}">Dashboard</a></li>
            <li><a href="{{ path('app_admin_forum_posts') }}" class="{{ current_menu == 'posts' ? 'active' : '' }}">Manage Posts</a></li>
            <li><a href="{{ path('app_admin_forum_comments') }}" class="{{ current_menu == 'comments' ? 'active' : '' }}">Manage Comments</a></li>
            <li><a href="{{ path('app_admin_forum_interactions') }}" class="{{ current_menu == 'interactions' ? 'active' : '' }}">Manage Interactions</a></li>
            <li><a href="{{ path('app_admin_forum_banned_words') }}" class="{{ current_menu == 'banned_words' ? 'active' : '' }}">Banned Words</a></li>
            <li><a href="{{ path('app_admin_forum_reports') }}" class="{{ current_menu == 'reports' ? 'active' : '' }}">Reported Content</a></li>
            <li style="margin-top:20px; border-top:1px solid rgba(255,255,255,0.1); padding-top:10px;"><a href="{{ path('app_admin_panel') }}" style="color:var(--primary-light);">Global Modules</a></li>
            <li><a href="{{ path('app_admin_forum_dashboard') }}">Forum</a></li>
            <li><a href="{{ path('app_admin_startup_dashboard') }}">Startup</a></li>
            <li><a href="{{ path('app_admin_evaluation_dashboard') }}">Evaluation</a></li>
            <li><a href="{{ path('app_admin_users') }}">Users</a></li>
            <li><a href="{{ path('app_admin_mentorship') }}">Mentorship</a></li>
            <li><a href="{{ path('app_admin_reclamations') }}">Reclamation & Response</a></li>
        </ul>
        <div class="sidebar-footer">
            <a href="{{ path('app_logout') }}" class="logout-btn glow-btn">Sign Out</a>
        </div>
    </aside>

    <!-- MAIN CONTENT -->
    <div class="main-content">
        <!-- TOPBAR -->
        <header class="topbar">
            <div class="topbar-left">
                <h2>Reported Content</h2>
            </div>
            <div class="topbar-right">
                <button id="themeToggleBtn" class="btn-pill" style="border: 2px solid var(--primary-light); background: transparent; color: var(--primary-light);">🌓</button>
            </div>
        </header>

        <main class="dashboard-content" style="position: relative; z-index: 2;">
            
            {% for message in app.flashes('success') %}
                <div style="background: rgba(46, 213, 115, 0.12); color: #1e8449; padding: 15px; border-radius: 8px; margin-bottom: 20px;">
                    {{ message }}
                </div>
            {% endfor %}
            {% for message in app.flashes('error') %}
                <div style="background: rgba(255, 71, 87, 0.12); color: #c0392b; padding: 15px; border-radius: 8px; margin-bottom: 20px;">
                    {{ message }}
                </div>
            {% endfor %}

            <div class="card" style="width: 100%;">
                <div style="overflow-x: auto;">
                    <table style="width: 100%; border-collapse: collapse; text-align: left;">
                        <thead>
                            <tr style="border-bottom: 1px solid rgba(255,255,255,0.1);">
                                <th style="padding: 15px; color: #b0a8d6;">Type</th>
                                <th style="padding: 15px; color: #b0a8d6;">Author</th>
                                <th style="padding: 15px; color: #b0a8d6;">Content</th>
                                <th style="padding: 15px; color: #b0a8d6;">Reporter Reason</th>
                                <th style="padding: 15px; color: #b0a8d6;">Date</th>
                                <th style="padding: 15px; color: #b0a8d6; text-align: right;">Actions</th>
                            </tr>
                        </thead>
                        <tbody>
                            {% for row in reportsData %}
                                <tr style="border-bottom: 1px solid rgba(255,255,255,0.05);">
                                    <td style="padding: 15px;">
                                        <span style="padding: 4px 8px; border-radius: 4px; background: rgba(108,92,231,0.2); color: var(--primary-light);">{{ row.report.targetType|capitalize }}</span>
                                    </td>
                                    <td style="padding: 15px; font-weight: 500;">{{ row.authorName }}</td>
                                    <td style="padding: 15px;">{{ row.contentSnippet }}</td>
                                    <td style="padding: 15px; color: #e74c3c;">{{ row.report.reason }}</td>
                                    <td style="padding: 15px; color: #999; font-size: 0.9em;">{{ row.report.createdAt|date('Y-m-d H:i') }}</td>
                                    <td style="padding: 15px; text-align: right; display:flex; gap:10px; justify-content:flex-end;">
                                        <form action="{{ path('app_admin_forum_report_delete_content', {'id': row.report.id}) }}" method="post" onsubmit="return confirm('Delete this content and ignore the report?');">
                                            <button type="submit" class="btn-pill" style="padding: 5px 10px; font-size: 0.8rem; background: rgba(255,0,0,0.1); color: #ff4757; border: 1px solid #ff4757;">Delete Content</button>
                                        </form>
                                        <form action="{{ path('app_admin_forum_report_ban_user', {'id': row.report.id}) }}" method="post" onsubmit="return confirm('Ban the user who POSTED this content?');">
                                            <button type="submit" class="btn-pill" style="padding: 5px 10px; font-size: 0.8rem; background: #e74c3c; color: white; border: none;">Ban User</button>
                                        </form>
                                    </td>
                                </tr>
                            {% else %}
                                <tr>
                                    <td colspan="6" style="padding: 30px; text-align: center; color: #888;">No reported content! 🎉</td>
                                </tr>
                            {% endfor %}
                        </tbody>
                    </table>
                </div>
            </div>
            
        </main>
        
        <footer class="futuristic-footer" style="padding: 20px; text-align: center; border-top: 1px solid rgba(255, 255, 255, 0.1);">
            <p style="color: #b0a8d6;">&copy; {{ 'now'|date('Y') }} StartupFlow Admin.</p>
        </footer>
    </div>

    <script>
        document.addEventListener("DOMContentLoaded", function() {
            const toggleBtn = document.getElementById('themeToggleBtn');
            if (toggleBtn) {
                toggleBtn.addEventListener('click', () => {
                    const isDark = document.body.classList.contains('dark-mode');
                    if (isDark) {
                        document.body.classList.remove('dark-mode');
                        localStorage.setItem('admin_theme', 'light');
                    } else {
                        document.body.classList.add('dark-mode');
                        localStorage.setItem('admin_theme', 'dark');
                    }
                });
            }
        });
    </script>
</body>
</html>
