{% extends "FrontOffice/base.html.twig" %}

{% block title %}Edit Business Plan - StartupFlow{% endblock %}

{% block body %}
<div class="auth-container" style="min-height: 100vh; padding: 40px 20px;">
    <div class="auth-card" style="max-width: 800px; margin: 0 auto; border-radius: 20px;">
        
        <div style="margin-bottom: 30px; border-bottom: 1px solid rgba(255,255,255,0.1); padding-bottom: 20px;">
            <a href="{{ path('app_entrepreneur_startup_show', {'id': startup.startupID}) }}" style="color: var(--primary-light, #9d88ff); text-decoration: none; font-size: 0.9em; display: inline-block; margin-bottom: 15px;">&larr; Back to Startup</a>
            <h1 style="color: var(--primary, #6f56ff); margin: 0; font-size: 2.2em;">Edit Business Plan</h1>
            <p style="margin: 10px 0 0 0;">Update the business model for <strong>{{ startup.name }}</strong></p>
        </div>

        <form method="post" action="{{ path('app_entrepreneur_businessplan_edit', {'id': businessplan.businessPlanID}) }}" style="display: flex; flex-direction: column; gap: 20px;">
            <div style="display: grid; grid-template-columns: 1fr 1fr; gap: 25px;">
                <!-- Left Column -->
                <div style="display: flex; flex-direction: column; gap: 20px;">
                    <div>
                        <label class="field-label" style="font-weight: 500; font-size: 0.95em; display: block; margin-bottom: 8px;">Plan Title <span style="color: #e57373;">*</span></label>
                        <input type="text" name="title" class="pillField" value="{{ businessplan.title }}" style="width: 100%; border-radius: 12px;">
                    </div>
                    
                    <div>
                        <label class="field-label" style="font-weight: 500; font-size: 0.95em; display: block; margin-bottom: 8px;">Executive Summary</label>
                        <textarea name="description" rows="5" class="pillField" style="width: 100%; border-radius: 12px; resize: vertical;">{{ businessplan.description }}</textarea>
                    </div>

                    <div>
                        <label class="field-label" style="font-weight: 500; font-size: 0.95em; display: block; margin-bottom: 8px;">Value Proposition</label>
                        <textarea name="valueProposition" rows="4" class="pillField" style="width: 100%; border-radius: 12px; resize: vertical;">{{ businessplan.valueProposition }}</textarea>
                    </div>

                    <div>
                        <label class="field-label" style="font-weight: 500; font-size: 0.95em; display: block; margin-bottom: 8px;">Business Model</label>
                        <textarea name="businessModel" rows="4" class="pillField" style="width: 100%; border-radius: 12px; resize: vertical;">{{ businessplan.businessModel }}</textarea>
                    </div>
                </div>

                <!-- Right Column -->
                <div style="display: flex; flex-direction: column; gap: 20px;">
                    <div>
                        <label class="field-label" style="font-weight: 500; font-size: 0.95em; display: block; margin-bottom: 8px;">Market Analysis</label>
                        <textarea name="marketAnalysis" rows="4" class="pillField" style="width: 100%; border-radius: 12px; resize: vertical;">{{ businessplan.marketAnalysis }}</textarea>
                    </div>

                    <div>
                        <label class="field-label" style="font-weight: 500; font-size: 0.95em; display: block; margin-bottom: 8px;">Marketing Strategy</label>
                        <textarea name="marketingStrategy" rows="4" class="pillField" style="width: 100%; border-radius: 12px; resize: vertical;">{{ businessplan.marketingStrategy }}</textarea>
                    </div>

                    <div>
                        <label class="field-label" style="font-weight: 500; font-size: 0.95em; display: block; margin-bottom: 8px;">Financial Forecast</label>
                        <textarea name="financialForecast" rows="4" class="pillField" style="width: 100%; border-radius: 12px; resize: vertical;">{{ businessplan.financialForecast }}</textarea>
                    </div>
                    
                    <div style="display: grid; grid-template-columns: 1fr 1fr; gap: 15px;">
                        <div>
                            <label class="field-label" style="font-weight: 500; font-size: 0.95em; display: block; margin-bottom: 8px;">Funding Req ($)</label>
                            <input type="number" step="1000" name="fundingRequired" value="{{ businessplan.fundingRequired }}" class="pillField" style="width: 100%; border-radius: 12px;">
                        </div>
                        <div>
                            <label class="field-label" style="font-weight: 500; font-size: 0.95em; display: block; margin-bottom: 8px;">Status</label>
                            <select name="status" class="pillField" style="width: 100%; border-radius: 12px;">
                                <option value="Draft" {% if businessplan.status == 'Draft' %}selected{% endif %}>Draft</option>
                                <option value="Published" {% if businessplan.status == 'Published' %}selected{% endif %}>Published</option>
                                <option value="Seeking Feedback" {% if businessplan.status == 'Seeking Feedback' %}selected{% endif %}>Seeking Feedback</option>
                            </select>
                        </div>
                    </div>

                    <div>
                        <label class="field-label" style="font-weight: 500; font-size: 0.95em; display: block; margin-bottom: 8px;">Timeline Horizon</label>
                        <input type="text" name="timeline" value="{{ businessplan.timeline }}" class="pillField" style="width: 100%; border-radius: 12px;">
                    </div>
                </div>
            </div>

            <div style="border-top: 1px solid rgba(255,255,255,0.1); padding-top: 25px; margin-top: 10px; text-align: right;">
                <button type="submit" class="pill-btn glow-btn" style="background: linear-gradient(135deg, var(--primary, #6f56ff), #b58bff); padding: 15px 40px; font-size: 1.1em; border: none; cursor: pointer;">Update Plan</button>
            </div>
        </form>
    </div>
</div>
{% endblock %}
